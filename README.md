ecc folder is a pack of module.

ecc.py is a python realization of a scalar multiplication algorithm.

ecc.sv is a not optimized and old testing variant of realization of this algorithm, testbench used for it is testbench.py (does not work right now because there were some changes to code).

test.sv testing file

main.sv full assembly of all module + topmodule, testbench for it testbench_main.py. fully works (there are some commented test for the modules)
